// A simple test to check if math works
test('1 + 1 should equal 2', () => {
    expect(1 + 1).toBe(2);
});

// A dummy test to check if the app name is correct
test('App name should be Backend', () => {
    const appName = "Backend";
    expect(appName).toBe("Backend");
});